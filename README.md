# Google Maps Task Advanced

Drawing on Google Maps

## Installation
Gulp requires [Node.js and NPM](https://nodejs.org/) to run.
Install the dependencies and devDependencies and start the server

```sh
$ npm install
$ gulp
```